<!--pages/api/api.wxml-->
  <view wx:for="{{urlMap}}" wx:for-index="k" wx:for-item="v" class="btn-area">
    <navigator url="{{v.url}}?title={{v.title}}" open-type="navigate" hover-class="navigator-hover">{{k+1}}. {{v.title}}</navigator>
  </view>

